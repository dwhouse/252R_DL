# ============================================
# 252R_DL Project Dependencies
# ============================================
# Install with: uv pip install -r requirements.txt
#
# Main packages (직접 설치한 패키지들)
# --------------------------------------------
ipykernel==7.1.0          # Jupyter 커널 지원
llama-index==0.14.7
docx2txt==0.9
# --------------------------------------------
# 새 패키지를 설치할 때:
# 1. uv pip install 패키지명
# 2. 이 파일의 "Main packages" 섹션에 직접 추가
# 
# 전체 의존성 고정이 필요한 경우:
# uv pip freeze > requirements.lock
# --------------------------------------------
